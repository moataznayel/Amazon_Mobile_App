import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    marginHorizontal: 5,
    padding: 10,
    borderWidth: 1,
    borderColor: "#ddd",
    borderRadius: 5,
    flexDirection: "row",
    justifyContent: "space-between",
    marginBottom: 5,
  },

  text: { fontWeight: "bold", color: "#007185", marginRight: 3 },
});
